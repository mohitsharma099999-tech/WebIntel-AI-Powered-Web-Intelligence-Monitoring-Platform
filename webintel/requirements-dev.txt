# =====================================================================
# Development dependencies
# =====================================================================
# Install alongside the runtime requirements:
#   pip install -r requirements.txt -r requirements-dev.txt
# =====================================================================

# ---------------------------------------------------------------------
# Core test runner
# ---------------------------------------------------------------------
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.25
pytest-cov>=4.1,<6.0
pytest-mock>=3.12,<4.0
pytest-xdist>=3.5,<4.0                # parallel test execution (-n auto)
pytest-timeout>=2.2,<3.0              # prevent hanging tests
pytest-env>=1.1,<2.0                  # set env vars for tests
pytest-sugar>=1.0,<2.0                # nicer test output

# HTTP client for FastAPI / async endpoint tests
httpx>=0.27,<0.29
anyio>=4.2,<5.0

# ---------------------------------------------------------------------
# Code quality: formatting, linting, import sorting
# ---------------------------------------------------------------------
black>=24.3,<25.0
isort>=5.13,<6.0
ruff>=0.4,<0.7
flake8>=7.0,<8.0
flake8-bugbear>=24.2,<25.0
flake8-comprehensions>=3.14,<4.0

# ---------------------------------------------------------------------
# Static type checking
# ---------------------------------------------------------------------
mypy>=1.9,<2.0
types-requests>=2.31,<3.0
types-python-dateutil>=2.9,<3.0

# ---------------------------------------------------------------------
# Security / dependency auditing
# ---------------------------------------------------------------------
bandit>=1.7,<2.0
pip-audit>=2.7,<3.0
safety>=3.1,<4.0

# ---------------------------------------------------------------------
# Pre-commit hooks
# ---------------------------------------------------------------------
pre-commit>=3.6,<4.0

# ---------------------------------------------------------------------
# Migrations (Alembic runs via runtime deps, but handy in dev)
# ---------------------------------------------------------------------
alembic>=1.13,<2.0

# ---------------------------------------------------------------------
# Debugging & profiling
# ---------------------------------------------------------------------
ipython>=8.20,<9.0
ipdb>=0.13,<1.0
py-spy>=0.3.14,<1.0

# ---------------------------------------------------------------------
# Test factories & fakes
# ---------------------------------------------------------------------
faker>=24.0,<27.0
factory-boy>=3.3,<4.0
freezegun>=1.4,<2.0
respx>=0.21,<1.0                      # mock httpx requests
aioresponses>=0.7.6,<1.0              # mock aiohttp requests

# ---------------------------------------------------------------------
# Coverage reporting (HTML + terminal)
# ---------------------------------------------------------------------
coverage[toml]>=7.4,<8.0

# ---------------------------------------------------------------------
# Docs (optional)
# ---------------------------------------------------------------------
mkdocs>=1.5,<2.0
mkdocs-material>=9.5,<10.0
mkdocstrings[python]>=0.24,<1.0

# ---------------------------------------------------------------------
# Load testing (optional, for scraping throughput tests)
# ---------------------------------------------------------------------
locust>=2.24,<3.0
